{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "",
  "title": "L0170",
  "description": "Data transformation pipeline output. The result of a pipeline is arbitrary transformed data — an array of rows, an object, or a scalar — so any JSON-compatible value is valid.",
  "type": ["object", "array", "string", "number", "boolean", "null"]
}
